# =============================================================================
# Environment Variables - Development
# =============================================================================
# Este arquivo contém todas as variáveis de ambiente necessárias para
# desenvolvimento local. NÃO commite este arquivo com valores reais.
# Use .env.example como template.
# =============================================================================

# -----------------------------------------------------------------------------
# Application
# -----------------------------------------------------------------------------
NODE_ENV=development
APP_NAME=nestjs-app
APP_PORT=3000

# -----------------------------------------------------------------------------
# Database (PostgreSQL)
# -----------------------------------------------------------------------------
DB_HOST=db                           # Nome do serviço no docker-compose
DB_PORT=5432
DB_DATABASE=dev_db
DB_USER=dev_user
DB_PASSWORD=dev_pass                 # ⚠️ Senha fraca - APENAS para dev local
DB_SYNCHRONIZE=false                 # ⚠️ NUNCA use true em produção
DB_LOGGING=true
DB_CONTAINER=postgres-db

# Connection String (alternativa)
# DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_DATABASE}

# -----------------------------------------------------------------------------
# Redis
# -----------------------------------------------------------------------------
REDIS_HOST=redis                     # Nome do serviço no docker-compose
REDIS_PORT=6379
REDIS_PASSWORD=                      # Vazio para desenvolvimento
REDIS_DB=0
REDIS_CONTAINER=redis-cache

# Connection String (alternativa)
# REDIS_URL=redis://${REDIS_HOST}:${REDIS_PORT}/${REDIS_DB}

# -----------------------------------------------------------------------------
# RabbitMQ (Opcional)
# -----------------------------------------------------------------------------
RABBITMQ_HOST=rabbitmq               # Nome do serviço no docker-compose
RABBITMQ_PORT=5672
RABBITMQ_MGMT_PORT=15672
RABBITMQ_USER=dev_user
RABBITMQ_PASS=dev_pass               # ⚠️ Senha fraca - APENAS para dev local
RABBITMQ_VHOST=/
RABBITMQ_CONTAINER=rabbitmq

# Connection String (alternativa)
# RABBITMQ_URI=amqp://${RABBITMQ_USER}:${RABBITMQ_PASS}@${RABBITMQ_HOST}:${RABBITMQ_PORT}/${RABBITMQ_VHOST}

# -----------------------------------------------------------------------------
# JWT / Auth
# -----------------------------------------------------------------------------
JWT_SECRET=dev-secret-change-in-prod # ⚠️ NUNCA use em produção
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d

# -----------------------------------------------------------------------------
# API / CORS
# -----------------------------------------------------------------------------
API_PREFIX=/api
CORS_ORIGIN=http://localhost:3001    # Frontend URL
CORS_CREDENTIALS=true

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
LOG_LEVEL=debug                      # verbose | debug | log | warn | error
LOG_PRETTY=true                      # Pretty print logs (dev only)

# -----------------------------------------------------------------------------
# Third-party Services (Exemplos)
# -----------------------------------------------------------------------------
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-key
# AWS_SECRET_ACCESS_KEY=your-secret

# SENDGRID_API_KEY=your-key
# SENDGRID_FROM_EMAIL=noreply@example.com

# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...

# -----------------------------------------------------------------------------
# Feature Flags (Opcional)
# -----------------------------------------------------------------------------
# FEATURE_NEW_UI=true
# FEATURE_BETA_ENDPOINT=false

# -----------------------------------------------------------------------------
# Performance / Debugging
# -----------------------------------------------------------------------------
# TYPEORM_LOGGING=true
# DEBUG=*                            # Debug all modules
# DEBUG=nestjs:*                     # Debug NestJS only

# -----------------------------------------------------------------------------
# IMPORTANTE:
# 1. Copie este arquivo para .env:
#    cp envs/.env.example envs/.env
#
# 2. Edite .env com valores apropriados para seu ambiente local
#
# 3. NUNCA commite .env com credenciais reais
#
# 4. Para produção, use secret managers (AWS Secrets Manager, etc.)
# =============================================================================
